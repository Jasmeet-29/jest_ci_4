test('adds 1 + 2 to equal 3', () => {
    expect(1 + 2).toBe(3);
  });

  test('subtracts 5 - 3 to equal 3', () => {
    expect(5 - 3).toBe(2);
  });

  test('multiply 5 * 3 to equal 15', () => {
    expect(5 * 3).toBe(15);
  });